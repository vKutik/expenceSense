# 🚀 Деплой на Vercel

## Подготовка к деплою

### 1. Установка Vercel CLI
```bash
npm install -g vercel
```

### 2. Логин в Vercel
```bash
vercel login
```

## Переменные окружения

В настройках проекта Vercel добавьте:

### Environment Variables:
- `BOT_TOKEN`: `8319629279:AAERWVdXipQIoqZR_OPd6RtcFHEb2PNvMG4`
- `FLASK_ENV`: `production`
- `FLASK_DEBUG`: `False`

## Структура файлов для деплоя:

```
snake/
├── api/
│   └── index.py          # Entry point для Vercel
├── templates/
│   └── index.html        # HTML шаблоны
├── models.py             # Модели данных
├── repository.py         # Репозиторий
├── services.py           # Сервисы
├── controllers.py        # Контроллеры
├── observers.py          # Наблюдатели
├── app.py               # Основное приложение
├── vercel.json          # Конфигурация Vercel
├── requirements.txt     # Зависимости Python
└── DEPLOYMENT.md        # Эта инструкция
```

## Команды для деплоя:

### 1. Инициализация проекта
```bash
vercel init
```

### 2. Деплой
```bash
vercel
```

### 3. Продакшн деплой
```bash
vercel --prod
```

## После деплоя:

1. **Обновите бота Telegram** с новым URL:
   - Замените `http://localhost:5000` на ваш Vercel URL
   - Например: `https://your-app-name.vercel.app`

2. **Настройте Webhook** (если используется):
   - Обновите URL в настройках бота

3. **Проверьте работу**:
   - Откройте URL в браузере
   - Проверьте API endpoints

## Возможные проблемы:

### 1. Ошибки импорта
- Убедитесь, что все файлы находятся в правильных директориях
- Проверьте пути в `api/index.py`

### 2. Переменные окружения
- Убедитесь, что все переменные добавлены в настройках Vercel
- Проверьте названия переменных

### 3. Таймауты
- Vercel имеет лимит времени выполнения
- Для долгих операций рассмотрите использование внешних сервисов

## Дополнительные настройки:

### Для продакшна:
- Настройте базу данных (например, PostgreSQL)
- Добавьте логирование
- Настройте мониторинг

### Безопасность:
- Не храните токены в коде
- Используйте HTTPS
- Настройте CORS правильно
